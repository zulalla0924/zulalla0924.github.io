---
import Layout from "../layouts/Layout.astro";
import Pagination from "../components/Pagination.astro";
import type { ShortPageData } from "../shared/type";
import config from "urodele.config";

interface Props {
  posts: ShortPageData[];
  page: number;
  pages: number;
}

const { posts, page, pages } = Astro.props;
const tags = ['index', 'ai', 'vibecoding', 'travel', 'offgrid', 'book', 'music', 'movie', 'blogmarks', 'aiskills'];

function formatDate(dateStr: string) {
  return new Date(dateStr).toISOString().split('T')[0];
}
---

<Layout title={config.head.title}>
  <header class="site-header">
    <h1 class="blog-title">Less is more</h1>
    <p class="blog-desc">
      <span class="text-en">A digital garden for thoughts, notes, and fragments.</span>
      <span class="text-cn">一念空灭，无处可逃；心无放逸，众善不失。</span>
    </p>
  </header>
  
  <main>
    <div class="content">
      <h2 class="section-title">Most recent posts</h2>
      
      <ul class="post-list">
        {
          posts.map((item) => (
            <li class="post-item">
              <time class="post-date">{formatDate(item.createTime)}</time>
              <a href={`/post/${item.id}`} class="post-title">{item.title}</a>
            </li>
          ))
        }
      </ul>
      
      <a href="/stream" class="view-more">查看更多碎片思考，请前往 Stream →</a>
      
      <Pagination page={page} pages={pages} />
      
      <section class="tags-section">
        <div class="tags">
          {tags.map(tag => (
            <a href={`/tag/${encodeURIComponent(tag)}`}>
              {tag === 'index' ? '🔍' : `#${tag}`}
            </a>
          ))}
        </div>
      </section>
    </div>
  </main>
</Layout>

<style>
  .site-header { 
    text-align: center; 
    padding: 3rem 0 2rem; 
    border-bottom: 1px dashed #292e42; 
  }
  .blog-title { 
    font-size: 1.8rem; 
    font-weight: 600; 
    color: #c8d3f5; 
    margin-bottom: 0.8rem; 
    letter-spacing: -0.5px;
  }
  
  .blog-desc { 
    color: #565f89; 
    font-size: 0.95rem; 
    font-style: italic; 
    max-width: 500px;
    margin: 0 auto;
    line-height: 1.6;
    position: relative;
    cursor: pointer;
  }
  
  .blog-desc .text-cn,
  .blog-desc .text-en {
    display: block;
    transition: opacity 0.3s ease;
  }
  
  .blog-desc .text-cn {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    opacity: 0;
    pointer-events: none;
  }
  
  .blog-desc:hover .text-en {
    opacity: 0;
  }
  
  .blog-desc:hover .text-cn {
    opacity: 1;
  }
  
  main {
    display: flex;
    justify-content: center;
    width: 100%;
    padding: 0 16px;
  }
  
  .content {
    width: 100%;
    max-width: 720px;
    padding: 0;
  }
  
  .section-title { 
    font-size: 1.1rem; 
    font-weight: 600; 
    color: #c8d3f5; 
    margin: 2rem 0 1rem; 
    padding-bottom: 0.5rem; 
    border-bottom: 1px dashed #292e42; 
  }
  
  .post-list { 
    list-style: none; 
    padding: 0; 
    margin: 0; 
  }
  
  .post-item { 
    display: flex; 
    align-items: baseline; 
    gap: 1.5rem; 
    padding: 0.35rem 0; 
  }
  
  .post-item:hover { 
    background: rgba(74, 158, 255, 0.05); 
  }
  
  .post-date { 
    font-family: "SF Mono", Monaco, monospace; 
    font-size: 0.8rem; 
    color: #565f89; 
    min-width: 90px; 
    flex-shrink: 0; 
  }
  
  .post-title { 
    color: #7aa2f7; 
    font-size: 0.95rem; 
    text-decoration: none; 
  }
  
  .post-title:hover { 
    color: #c8d3f5; 
    text-decoration: underline; 
  }
  
  .view-more { 
    display: block; 
    margin-top: 1.5rem; 
    color: #565f89; 
    font-size: 0.85rem; 
    font-style: italic; 
  }
  
  .view-more:hover { 
    color: #7aa2f7; 
  }
  
  .tags-section { 
    margin-top: 3rem; 
    padding-top: 2rem; 
    border-top: 1px dashed #292e42; 
    text-align: center; 
  }
  
  .tags { 
    display: flex; 
    flex-wrap: wrap; 
    justify-content: center; 
    gap: 1rem; 
  }
  
  .tags a { 
    color: #565f89; 
    font-size: 0.9rem; 
    text-decoration: none; 
  }
  
  .tags a:hover { 
    color: #7aa2f7; 
  }
</style>
